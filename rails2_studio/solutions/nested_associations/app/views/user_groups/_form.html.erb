<%= form_for(@user_group) do |f| %>
  <% if @user_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_group.errors.count, "error") %> prohibited this user_group from being saved:</h2>

      <ul>
      <% @user_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <h2> Members </h2>
  <div id='members'>
  <%= f.fields_for :members do |member_form| %>
     <%= render :partial => 'member_form', :locals => {:member_form => member_form }%>
  <% end %>
  </div>
  <%= button_to_add_a_member(f) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
